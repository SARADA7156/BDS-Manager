<!DOCTYPE html>
<html lang="ja">
    <head>
        <meta charset="UTF-8">
        <title>BDS Manager</title>
        <meta name="description" content="Webアプリの管理をする画面">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="icon" type="image/png" href="/imgs/bedrock_icon.webp">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
        <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
        <link rel="stylesheet" href="/css/style.css">
        <link rel="stylesheet" href="/css/components/header.css">
        <link rel="stylesheet" href="/css/pages/<%= style %>">
    </head>

    <body>
        <!-- ヘッダーのマークアップ -->
        <% if (typeof header !== "undefined" && header) { %>
            <header class="container-fluid">
                <div class="p-2 d-grid align-items-center header-content">
                    <%- include(`./components/${header}`) %>
                </div>
            </header>
        <% } %>

        <!-- メイン要素のマークアップ -->
        <main>
            <% if (typeof nav !== "undefined" && nav) { %>
                <%- include(`./components/${nav}`) %>
            <% } %>

            <% if (typeof page !== "undefined" && page) { %>
                <%- include(`./pages/${page}`) %>
            <% } %>
        </main>

        <!-- フッターのマークアップ -->
        <footer class="container-fluid justify-content-center d-grid">
            <div class="d-grid justify-content-center align-items-center information">
                <div class="d-flex justify-content-center BDS-emblem">
                    <img src="/imgs/bedrock_icon.webp" alt="BDS Manager icon" class="bedrock">
                    <h2 class="ms-2"><b>BDS</b> Manager</h2>
                </div>

                <p class="text-center">当Webアプリケーションは<code>SARADA7156</code>が管理・運営している個人用Webアプリケーションです。<br>管理者の許可なく不正にログイン・操作することは一切禁じます。</p>
            </div>

            <small class="text-center text-white-50">&copy;2025 SARADA7156</small>
        </footer>
    </body>
</html>
